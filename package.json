{
  "name": "oneiroi",
  "version": "1.0.0",
  "scripts": {
    "build": "npx vite build",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "devDependencies": {
    // ...
  }
}
